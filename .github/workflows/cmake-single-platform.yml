name: CMake

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            build_type: Release
            c_compiler: gcc
            cpp_compiler: g++
          - os: ubuntu-latest
            build_type: Release
            c_compiler: clang
            cpp_compiler: clang++
          - os: windows-latest
            build_type: Release
            c_compiler: cl
            cpp_compiler: cl
          - os: macos-latest
            build_type: Release
            c_compiler: clang
            cpp_compiler: clang++
            cmake_osx_architectures: 'x86_64;arm64'

    steps:
      - uses: actions/checkout@v4

      - name: Configure (non-macOS)
        if: matrix.os != 'macos-latest'
        run: cmake -B ${{ github.workspace }}/build -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -S ${{ github.workspace }}

      - name: Configure (macOS Universal)
        if: matrix.os == 'macos-latest'
        run: cmake -B ${{ github.workspace }}/build -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} "-DCMAKE_OSX_ARCHITECTURES=${{ matrix.cmake_osx_architectures }}" -S ${{ github.workspace }}

      - name: Build
        run: cmake --build ${{ github.workspace }}/build --config ${{ matrix.build_type }}

      - name: Test
        working-directory: ${{ github.workspace }}/build
        run: ctest --build-config ${{ matrix.build_type }} --output-on-failure

  # Exercise the LLVM backend and both supported object formats.
  llvm:
    name: LLVM backend (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install LLVM (Linux)
        if: runner.os == 'Linux'
        run: |
          curl -fsSL -o llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 20
          sudo apt-get install -y llvm-20-dev

      - name: Install Windows toolchain
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-ninja

      - name: Install LLVM (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: >
          pacman --noconfirm -U
          https://repo.msys2.org/mingw/ucrt64/mingw-w64-ucrt-x86_64-llvm-20-20.1.8-4-any.pkg.tar.zst

      - name: Configure (Linux)
        if: runner.os == 'Linux'
        run: >
          cmake -B ${{ github.workspace }}/build -S ${{ github.workspace }}
          -DCMAKE_BUILD_TYPE=Release
          -DDOLRECOMP_ENABLE_LLVM=ON
          -DLLVM_DIR=/usr/lib/llvm-20/lib/cmake/llvm

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: >
          cmake -G Ninja -B build -S .
          -DCMAKE_BUILD_TYPE=Release
          -DDOLRECOMP_ENABLE_LLVM=ON
          -DLLVM_DIR=/ucrt64/opt/llvm-20/lib/cmake/llvm

      - name: Build (Linux)
        if: runner.os == 'Linux'
        run: cmake --build ${{ github.workspace }}/build --config Release

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: cmake --build build

      - name: Test (Linux)
        if: runner.os == 'Linux'
        working-directory: ${{ github.workspace }}/build
        run: ctest --build-config Release --output-on-failure

      - name: Test (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: ctest --test-dir build --output-on-failure

      - name: Upload Entire Build Folder Artifact
        uses: actions/upload-artifact@v4
        with:
          name: DolRecomp-Complete-Build-Directory
          path: build/
